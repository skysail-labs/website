{
  "framework": "nextjs",
  "installCommand": "bun install",
  "buildCommand": "cd docs && bun install && bun run build && mkdir -p ../public/docs && cp -r build/* ../public/docs/ && cd .. && bun run build",
  "outputDirectory": ".next"
}
